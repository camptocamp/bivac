package providers

/*
import (
	"fmt"
)

func PrepareBackups() {
	if isMySQL(volumePath) {

	}

	return
}

func listFiles(searchDir string) (files []string, err error) {
	err = filepath.Walk(searchDir, func(path string, info os.FileInfo, err error) error {
		if err != nil {
			return err
		}
		files = append(files, path)
		return nil
	})
	return
}
*/
